# import requests
# ## Put your own REST endpoint
# url = "https://endpoint-model-a.westeurope.inference.ml.azure.com/score"
# payload={}
# files=[
#   ('image',('file',open('sglbl.jpg','rb'),'application/octet-stream'))
# ]
# headers = {
#   'Authorization': 'Bearer api_key'
# }
# response = requests.request("POST", url, headers=headers, files=files)
# print(response.text)

a = [1,2,3,4]
print(a)